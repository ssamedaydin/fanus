name: fanus_server
description: "Fanus ortak odak odaları için küçük REST + WebSocket demo sunucusu."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.12.0
resolution: workspace
dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3
