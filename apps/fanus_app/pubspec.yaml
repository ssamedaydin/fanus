name: fanus_app
description: "Fanus — kişisel odak ve dijital denge uygulaması."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fanus_core:
    path: ../../packages/fanus_core
  fanus_design:
    path: ../../packages/fanus_design
  fanus_location:
    path: ../../packages/fanus_location
  hooks_riverpod: ^2.6.1
  flutter_hooks: ^0.21.3+1
  go_router: ^18.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
